[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "worker-bee"
version = "0.1.0"
description = "Belief-driven orchestrator for small-context worker bees"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = ["ftl-reasons", "prompt_toolkit"]

[project.optional-dependencies]
test = ["pytest", "pytest-cov"]
ollama = ["httpx>=0.27"]
openai = ["openai>=1.0"]
tokens = ["tiktoken>=0.7"]

[project.scripts]
worker-bee = "worker_bee.cli:main"

[tool.setuptools]
packages = ["worker_bee"]

[tool.pytest.ini_options]
testpaths = ["tests"]
